## Убрать срок выполнения с карточек проектов

В `src/routes/projects.tsx` в блоке карточек удалить `<span>` с иконкой `Clock` и `{p.term}` (строки 142–145). Локация остаётся.

В лайтбоксе (модалке) срок оставить — там это полезная деталь проекта.
